<resources>

    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="android:Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/CustomActionBar</item>
        <item name="android:actionBarSize">50dp</item>
        <item name="android:actionButtonStyle">@style/CustomeActionButtonStyle</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowBackground">@drawable/background</item>
    </style>

    <style name="CustomActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:titleTextStyle">@style/CustomText</item>
    </style>

    <style name="CustomText" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/colorTitle</item>
        <item name="android:fontFamily">@font/headland_one</item>
        <item name="android:textSize">20dp</item>
    </style>

    <style name="CustomeActionButtonStyle" parent="@android:style/Widget.ActionButton">
        <item name="android:layout_width">fill_parent</item>
    </style>


</resources>
